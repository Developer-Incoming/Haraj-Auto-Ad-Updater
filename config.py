auth = "auth"   # Bearer Auth, get it from your browser's Network tab fetch requests.
postId = 0      # On most cases remove the two digits on the far left.
english = True  # Arabic doesn't work well with the Windows terminal.
